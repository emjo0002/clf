---

---

<html lang="da">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<header>
			<div class="menu_grid">
				<div><a href="../pages/index.astro">
					<img src="../public/logo2.png" alt="logo">
				</a>
				</div>
				<div class="sprog">
					<p class="dansk">DA</p>
					<p>EN</p></div>
				<div class="global_menu">
					<p class="lilla">For besøgende</p>
					<p>For kunstnere</p>
					<p>For sponsorer</p>
					<p>OM</p>
				</div>
				<div class="lokal_menu">
					<a class="no_underline" href="udstillinger.astro">UDSTILLINGER</a>
					<p class="rykke_op">FESTIVAL MAP</p>
					<p class="rykke_op">APP</p>
					<li class="dropdown">
						<a href="#" class="dropbtn">UDFORSK</a>
						<div class="dropdown-content">
						  <a href="events_astro">Events 2024</a>
						  <a href="#">Guidede ture 2024</a>
						  <a href="#">Hotel/Restaurant</a>
						</div>
					  </li>
				</div>
				<div class="burger">
                    <div class="line"></div>
                    <div class="line"></div>
                    <div class="line"></div>
                </div>
			</div>
		</header>
		<main>
			<h1>Astro</h1>
		</main>
		<script src="../../public/burger.js"></script>
	</body>

	<style>
@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;900&display=swap');
/* Dropdown menu */
/* Style the dropdown button */
.dropbtn {
  display: inline-block;
  text-decoration: none;
}

/* Dropdown container - needed to position the dropdown content */
.dropdown {
  display: inline-block;
  position: relative;
}

/* Dropdown content (hidden by default) */
.dropdown-content {
  display: none;
  position: absolute;
  background-color: #000000;
  min-width: 160px;
  z-index: 1;
  font-weight: lighter;
}

/* Links inside the dropdown */
.dropdown-content a {
  display: block;
  color: rgb(255, 255, 255);
  padding: 12px 16px;
  text-decoration: none;
}

/* Change color of dropdown links on hover */
.dropdown-content a:hover {
  background-color: #000000;
}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {
  display: block;
}

header {
	background-color: black;
}

p, a {
	font-family: "Roboto", sans-serif;
	color: white;
}

.no_underline {
	text-decoration: none;
}

.burger {
    cursor: pointer;
    display: none; /* Skjuler burgerikonet som standard */
}

.burger .line {
    width: 25px;
    height: 3px;
    background-color: rgb(255, 255, 255);
    margin: 5px;
}

@media screen and (max-width: 768px) {
.menu_grid {
    display: grid;
    grid-template-columns: auto auto auto auto;
    align-items: center;
}

.global_menu, .lokal_menu {
    display: flex;
	justify-content: flex-end;
	display: none; /* Skjuler menuerne på mobilvisning */
}
img {
	width: 30%;
}
.sprog {
        display: none; /* Skjules på mobilversion */
    }
.burger {
        display: block; /* Vis burgerikonet på mobilvisning */
    }

.global_menu.active, .lokal_menu.active {
        display: flex;
        flex-direction: column;
        position: absolute;
        background-color: #f9f9f9;
        width: 100%;
        top: 100%;
        left: 0;
        padding: 10px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
    }
}

/* DESKTOP */
@media screen and (min-width: 800px) {
.rykke_op {
	margin-top: -1px;
}

.lilla {
	color: #8B61F7;
}

/* Hoover effekt */
.no_underline {
  text-decoration: none;
  color: rgb(255, 255, 255); /* Standard farve på menuen */
  position: relative; /* Position relativ for at tilføje den lilla streg */
}

.no_underline:hover {
  color: #8B61F7; /* Ændrer farven på menuen, når musen svejses over */
}

.no_underline:hover::after {
  content: '';
  position: absolute;
  bottom: 40px; /* Justerer positionen af den lilla streg */
  left: 0;
  width: 100%;
  height: 2px;
  background-color: #8B61F7; /* Farve på den lilla streg */
}
.dansk {
	font-weight: bold;
	margin-right: 10px;
}
.sprog {
  display: flex;
}

.global_menu {
	display: flex;
	gap: 30px;
	margin-left: 15px;
}
img {
	width: 25%;
}

.global_menu {
	font-size: 15px;
}

.lokal_menu {
	display: flex;
    justify-content: flex-end;
    grid-column: 2 / span 2;
    gap: 60px;
    margin-right: 60px;
	font-weight: bold;
}
.menu_grid {
	display: grid;
	grid-template-columns: 1fr 1fr 1fr;
	grid-template-rows: 1fr 1fr;
}
}
	</style>
</html>
